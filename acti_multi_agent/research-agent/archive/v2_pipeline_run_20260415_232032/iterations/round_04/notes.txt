Round 4 patches before run:
- main.py auto-loop no longer re-runs Phase 5 within the same iteration
- phase4 fallback inventory now stays contract-compliant without surfacing schema-failure text as content gaps
- scoring reviewer contexts compressed so all 7 beats remain visible
- phase3.5 spine metadata normalized for later beats
- coverage reviewer prompt aligned with 4-15prompt migration policy
